# P3 タスクリスト

日付: 2026-04-18

## 実装タスク

1. `p3-core/` パッケージ骨格を追加する
2. workspace bootstrap と JSON/JSONL persistence を実装する
3. model router を実装する
4. Ollama chat client を実装する
5. tool registry と tool executor を実装する
6. session queue と event append を実装する
7. turn loop と worker loop を実装する
8. dashboard snapshot と HTML renderer を実装する
9. dashboard API と message/snapshot/SSE 表示を実装する
10. CLI を実装する
11. fake backend ベースの runtime test を追加する
12. dashboard HTTP test を追加する
13. README を追加する
14. dashboard package 構成へ統一する
15. grounding judge の JSON verdict 化と raw trace 保存を実装する
16. `blocked` operation status と blocked reason 表示を実装する
17. 受動的な実況解説者 `observer_note` を実装する
18. `llm_output_issue` を実装し、tool_call JSON にならない中間失敗も解説対象にする
19. operation と後追い observer event を `turn_id` / `queue_id` で紐づける
20. LLM 出力失敗を `empty_output` / `missing_json_object` / `json_parse_error` / `length_truncated` / `invalid_tool_envelope` に分類し、runtime status と dashboard に出す
21. 行動モデルの prompt context を現在タスク中心に浄化し、古い assistant output と `observer_note` を再投入しない
22. `append_file` と `replace_text` を追加し、長いコード全文を単一 JSON tool 引数に入れる運用を避ける
23. turn 専用 LLM workspace を `workspaces/runs/<turn_id>/` に作成し、file/command tool の実行先にする
24. 解説者の独自短縮 timeout を廃止し、runtime `chat_timeout_seconds` に揃える
25. benchmark 的な coding prompt 専用の固定 scaffold fast path を持たない
26. controller/fast-path step では解説者 LLM を同期実行せず、本処理の進行を止めない

## 受け入れ条件

- temp workspace で bootstrap できる
- fake model で `user -> read_file -> write_file -> finish` が通る
- dashboard から message 投入後に snapshot が更新される
- status に current model / queue / latest events が出る
- finish block が SUCCESS と誤表示されず、`blocked` と理由で見える
- 解説者が step 単位で LLM/システムのやり取り、失敗要因、コンテキスト点検を表示する
- LLM 応答が tool_call JSON にならない場合も event と commentary が残る
- dashboard refresh 後も最新の解説者表示が維持される
- 長い `write_file` は拒否され、`append_file` chunk への誘導が tool_result に残る
- 途中切れ出力は `length_truncated` として `llm_output_issue` / runtime status に残る
- action prompt に古い別タスク、実況解説、失敗した長大 assistant output が混入しない
- tool 実行で作成されたファイルは P3 root ではなく turn 専用 LLM workspace に作られる
- dashboard に current/last LLM workspace が表示される
- 解説者の timeout は通常 runtime timeout と同じ扱いになる
- `迷路を実装して実行し表示して結果を見せて` が `write_file -> run_command -> finish` で成功し、stdout に S/G 付き迷路が出る
- controller/fast-path step は `llm_attempt_count = 0` として LLM 自由生成と区別される

## タスクリストレビュー

結論: 順序は妥当。

理由:

- 先に persistence/runtime を固め、UI を最後に被せる構成になっている
- test が末尾ではなく、runtime 完了後に直結している
- README を最後に回しており、実装に引きずられない
