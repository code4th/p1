# P4 タスクリスト (改訂版)

日付: 2026-04-21

## フェーズ 0: P4 プロジェクト作成 ＋ リファクタリング

- [x] 1. P3 (`p3-core/`) をコピーして `p4-core/` を作成する（P3 は凍結、変更しない）。
- [x] 2. package 名を `p3_core` → `p4_core` に変更する。
- [x] 3. テストが全件パスすることを確認し、機能差分がないことを保証する。
- [x] 4. `runtime.py` から observer 関連メソッドを `observer.py` に抽出する。
- [x] 5. `runtime.py` から grounding 関連メソッドを `grounding.py` に抽出する。
- [x] 6. `runtime.py` から terminal agent 関連メソッドを `terminal.py` に抽出する。
- [x] 7. deterministic shortcut は削除し、通常の LLM action loop に一本化する。
- [x] 8. `runtime.py` から prompt 構築関連メソッドを `prompts.py` に抽出する。
- [x] 9. `runtime.py` から LLM 通信・JSON修復関連メソッドを `llm_comm.py` に抽出する。
- [x] 10. `runtime.py` から guards 関連メソッドを `guards.py` に抽出する。
- [x] 11. リファクタリング後に全テスト（35件）が再度パスすることを確認し、挙動差分がないことを検証する。

### フェーズ 0 作業メモ
- P4プロジェクトを作成し、package rename と runtime 責務分離後に `python3 -m unittest discover -s tests` で38件パス。P3は未変更。



## フェーズ 1: フレーム基盤（in-memory 最小実装）

- [x] 12. `Frame` dataclass を定義する（`p4_core/frames.py`）。
- [x] 13. `FrameManager` クラスを実装する（`create_root_frame` / `open_child_frame` / `return_to_parent` / `current_frame` / `frame_stack` / `update_working_memory`）。
- [x] 14. `open_child_frame` と `return_to_parent` を LLM が選択できるアクションとして `tools.py` に追加する。ただし実装では kernel control action として特別扱いする。
- [x] 15. `prompts.py` を更新し、LLM が `open_child_frame` と `return_to_parent` をいつ使うべきかのガイダンスを system prompt に追加する。
- [x] 16. `_process_queue_item` にフレーム遷移アクションのハンドリングを追加する。`frame_opened` / `frame_returned` イベントを記録し、`active_frame_id` を適切に切り替える。
- [x] 17. `child_return` イベント型を追加し、親フレームの events に子フレームの `return_payload` を記録する。
- [x] 18. `_conversation_messages` をフレーム対応にする。現在フレームの `session_events` と親から継承した context summary を合成して返す。
- [x] 19. フレームの最大深さ制限（4階層）と、フレーム内最大ステップ数（15ステップ）の safety valve を実装する。
- [x] 20. 新規ユーザーメッセージによる既存フレームの abandon と root リセット処理を実装する。
- [x] 21. フレーム基盤のテストを追加する。root → child → return の流れが in-memory で通り、子の events が親に混入しないことを検証する。
- [x] 22. **ここまで** で in-memory のフレーム遷移が安定したことを確認する。

### フェーズ 1 作業メモ
- in-memory フレーム基盤を実装。root→child→return、子イベント隔離、深さ制限、子フレーム finish ブロックをテストで固定。



## フェーズ 2: 永続化と Working Memory

- [x] 23. フレーム状態の永続化を実装する（`state/frames/` に JSONL で保存）。`FrameManager` が起動時にファイルから状態を読み込めるようにする。
- [x] 24. 永続化のテストを追加する（セッションをまたいでもフレーム階層が復元されること）。
- [x] 25. `WorkingMemory` dataclass を定義する（`observations`, `current_focus`, `unresolved_questions`, `avoid_repeating` の4項目）。
- [x] 26. tool 実行後の working memory 自動更新ロジックを実装する（`read_file` → `observations`、`run_command` 失敗 → `avoid_repeating` 等）。
- [x] 27. working memory を prompt に注入するロジックを `prompts.py` に追加する。LLM が現在のフレーム状態を理解できるようにする。
- [x] 28. working memory を dashboard snapshot に公開する。
- [x] 29. working memory のテストを追加し、tool 実行後に正しく更新されることを確認する。

### フェーズ 2 作業メモ
- `state/frames/frames.jsonl` へのスナップショット永続化と WorkingMemory 自動更新を実装。


## フェーズ 3: Dashboard・Observer 拡張

- [x] 30. dashboard snapshot にフレーム階層表示を追加する。`frame_stack` と `current_frame_id` を含め、各フレームの working memory を閲覧できるようにする。
- [x] 31. dashboard HTML にフレームツリーの表示とフレーム選択 UI を追加する。
- [x] 32. observer にフレーム遷移の解説ロジックを追加する。`frame_opened` / `frame_returned` / `child_return` を解説する専用パスを実装する。
- [x] 33. dashboard 拡張のテストを追加する。

### フェーズ 3 作業メモ
- dashboard snapshot / HTML にフレーム階層を追加し、observer は frame_opened/frame_returned を解説する。



## フェーズ 4: 安定化・検証

- [x] 34. 単純タスク（`pwd` → `finish`）が引き続き動くことを確認する。
- [x] 35. 2階層タスク（root → child → return → finish）の live テストを実施する。
- [x] 36. 3階層以上の live テストを実施する。深さ制限超過時や safety valve の動作も確認する。
- [x] 37. 帰還イベント (`frame_returned`) と `child_return` イベントの記録をモニタリングする仕組みを追加し、後続の性能分析用にデータを保存する（帰還率は受け入れ基準ではなく分析用指標）。
- [x] 38. README を更新し、フレーム機能の使い方と互換境界を記述する。

### フェーズ 4 作業メモ
- 単純live: `/tmp/p4-live-check` で `pwd -> finish` 成功。
- 2階層live: 初回検証で controller finish が子フレーム内で発火するバグを発見し修正。修正後 `/tmp/p4-live-frame` で `frame_returned` / `child_return` / root finish を確認。
- 3階層live: `/tmp/p4-live-deep2` で `frame_opened=3`, `frame_returned=3`, `child_return=3`, `active_depth=0` を確認。
- 深さ制限live: `/tmp/p4-live-depth-limit` で level5 open が `frame_open_blocked / depth_limit` になることを確認。
- safety valve: unit test `test_child_frame_step_safety_valve_returns_to_parent` で15 step超過時の自動 return を固定。
- 帰還イベント monitoring: `FrameManager.snapshot()["metrics"]` で `frame_opened` / `frame_returned` / `child_return` / `active_depth` を公開。


## 受け入れ条件

### フェーズ 0

- P4 プロジェクトが作成され、テスト全件パス。リファクタリングによる機能差分が無いこと。
- P3 は未変更。

### フェーズ 1

- `open_child_frame` でフレームが開ける。
- `return_to_parent` で親に戻れる。
- 子フレームの `session_events` が親に混入しない。
- `return_to_parent` 後に親フレームの続行が可能。
- 子フレームの `return_payload` が `child_return` として記録される。
- 深さ 4 を超えるとエラーになる。
- 再帰フレームを使わないタスクは P3 と同じ動作になる。

### フェーズ 2

- フレーム状態がディスクに保存され、再開時に復元できる。
- `read_file` 実行後に `observations` にファイル名が追加される。
- `run_command` 失敗後に `avoid_repeating` にコマンドが追加される。
- prompt に working memory の状態が含まれる。

### フェーズ 3

- dashboard にフレーム階層が表示され、現在フレームの working memory と goal が確認できる。
- observer が `frame_opened` / `frame_returned` / `child_return` を解説する。

### フェーズ 4

- 単純タスク・2階層タスク・3階層タスクの live テストが成功し、深さ制限が正しく機能する。
- テスト全件パス。
- 帰還イベントの記録が取得できる。（帰還率はモニタリング指標であり、達成基準ではない。）
